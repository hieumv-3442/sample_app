<%= form_for(new_active_relationship, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <%= f.submit t("user.action.follow"), class: "btn btn-primary" %>
<% end %>
